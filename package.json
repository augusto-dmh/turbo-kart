{
  "name": "turbo-kart",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Turbo Kart — an AAA-quality arcade kart racer built with Three.js",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5173 --strictPort",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4173 --strictPort",
    "smoke": "node tests/smoke.mjs"
  },
  "dependencies": {
    "three": "^0.186.0"
  },
  "devDependencies": {
    "puppeteer-core": "^25.12.0",
    "vite": "^8.3.0"
  }
}
